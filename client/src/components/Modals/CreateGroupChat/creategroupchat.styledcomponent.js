import styled from 'styled-components';

export const CreateGroupChatModal = styled.form`
    width: max-content;
    z-index: 1;
    /* padding: 4rem; */
    /* display: flex; */
    /* margin: 0 auto; */
    /* min-height: 50%;
    height: auto; */
    /* flex-direction: column; */
    /* border-radius: 10px; */
    /* color: ${(props) => props.theme.colors.text}; */
    /* background-color: ${(props) => props.theme.colors.darkBackground}; */
`;
